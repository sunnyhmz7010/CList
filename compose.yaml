services:
  clist:
    image: ghcr.io/sunnyhmz7010/clist:latest
    container_name: clist
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - clist_data:/data

volumes:
  clist_data:
